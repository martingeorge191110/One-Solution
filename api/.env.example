# Database
DATABASE_URL="postgresql://user:password@localhost:5432/one_solutions?schema=public"

# App
PORT=5000
NODE_ENV=development

# CORS
WEB_ORIGIN=http://localhost:3000

# JWT — the code reads these four vars (see src/common/token/token.service.ts).
# Use DIFFERENT strong values for access vs refresh in production.
JWT_SECRET=replace-with-strong-random-secret
JWT_REFRESH_SECRET=replace-with-different-strong-random-secret
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Seed
SEED_SUPERADMIN_EMAIL=martingeorge191110@gmail.com
SEED_SUPERADMIN_PASSWORD=martinge1410!
